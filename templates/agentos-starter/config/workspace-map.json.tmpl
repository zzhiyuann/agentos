{
  "_comment": "Resolves Linear issues to local workspace directories. Checked in priority order: project:<name> → default. '~' is expanded to $HOME. Installed into ~/.aos/workspace-map.json.",
  "default": "{{WORKSPACE_BASE}}",

  "project:Example Project": "{{HOME}}/projects/example-project"
}
