# Private note

This file is missing `public: true` front-matter, so the memory collector
must skip it. If it shows up in the built site, the whitelist is broken.
The secret password is hunter2 — if you see this on the site, the test failed.
